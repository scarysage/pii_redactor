# FULLY PINNED LOCK FILE. Everything must work offline at runtime; no package
# here is permitted to phone home from the running app.
#
# The top section is the curated set of DIRECT dependencies (edit these).
# The 'Locked transitive dependencies' section at the bottom is generated
# from a verified install -- pinning the FULL tree so a fresh setup on the
# boss's machine installs the exact versions tested here (no silent drift).
# Do not hand-edit the bottom section; regenerate it if you change a direct
# dependency. Versions chosen for broad Python 3.10-3.12 compatibility.

# --- Core redaction engine ---
presidio-analyzer==2.2.355
presidio-anonymizer==2.2.355
spacy==3.7.5

# --- File extractors ---
pdfplumber==0.11.9
# pdfminer.six is pulled in by pdfplumber, but we pin it explicitly: versions
# before 20251230 have a pickle-based code-execution flaw (CVE-2025-64512 /
# CVE-2025-70559) that a malicious PDF can trigger. This is the parser every
# uploaded PDF goes through, so the pin is security-critical -- do not lower it.
pdfminer.six==20251230
python-docx==1.1.2
openpyxl==3.1.5

# --- UI ---
# Bumped 1.39.0 -> 1.58.0 to clear CVE-2026-33682 (Windows path traversal in
# component request handling, fixed in 1.54.0). NOTE: 1.54+ replaced Streamlit's
# Tornado server with a starlette/uvicorn stack. The built-in toolbar (Deploy
# button + 3-dot menu) MUST stay hidden -- the data-testid selectors in app.py
# were re-verified against this version. If you bump again, re-check those
# selectors against the new static JS bundle (see CLAUDE.md "Boundaries").
streamlit==1.58.0
# pillow is pulled in transitively (pdfplumber / reportlab / streamlit) but we
# pin it explicitly: versions < 12.2.0 carry several image/PDF parsing flaws
# (CVE-2026-42311 PSD memory corruption, CVE-2026-42310 PDF CPU DoS, etc.).
pillow==12.2.0

# --- Dev / test only (safe to keep installed; not invoked at runtime) ---
pytest==9.0.3

# ============================================================================
# Locked transitive dependencies (auto-generated; do not hand-edit).
# Pinned to the verified, currently-running version set.
# ============================================================================
altair==5.5.0
annotated-doc==0.0.4
annotated-types==0.7.0
anyio==4.13.0
attrs==26.1.0
azure-core==1.41.0
blinker==1.9.0
blis==0.7.11
cachetools==5.5.2
catalogue==2.0.10
certifi==2026.5.20
cffi==2.0.0
charset-normalizer==3.4.7
click==8.4.1
cloudpathlib==0.24.0
confection==0.1.5
cryptography==48.0.0
cymem==2.0.13
et_xmlfile==2.0.0
filelock==3.29.0
gitdb==4.0.12
GitPython==3.1.50
h11==0.16.0
httptools==0.8.0
idna==3.17
iniconfig==2.3.0
itsdangerous==2.2.0
Jinja2==3.1.6
jsonschema==4.26.0
jsonschema-specifications==2025.9.1
langcodes==3.5.1
lxml==6.1.1
markdown-it-py==4.2.0
MarkupSafe==3.0.3
mdurl==0.1.2
murmurhash==1.0.15
narwhals==2.21.2
numpy==1.26.4
packaging==24.2
pandas==2.3.3
phonenumbers==8.13.55
pluggy==1.6.0
preshed==3.0.13
protobuf==5.29.6
pyarrow==24.0.0
pycparser==3.0
pycryptodome==3.23.0
pydantic==2.13.4
pydantic_core==2.46.4
pydeck==0.9.2
Pygments==2.20.0
pypdfium2==5.8.0
python-dateutil==2.9.0.post0
python-multipart==0.0.30
pytz==2026.2
PyYAML==6.0.3
referencing==0.37.0
regex==2026.5.9
requests==2.34.2
requests-file==3.0.1
rich==13.9.4
rpds-py==2026.5.1
setuptools==82.0.1
shellingham==1.5.4
six==1.17.0
smart_open==7.6.1
smmap==5.0.3
spacy-legacy==3.0.12
spacy-loggers==1.0.5
srsly==2.5.3
starlette==1.2.1
tenacity==9.1.4
thinc==8.2.5
tldextract==5.3.1
toml==0.10.2
tqdm==4.67.3
typer==0.26.3
typer-slim==0.24.0
typing-inspection==0.4.2
typing_extensions==4.15.0
tzdata==2026.2
urllib3==2.7.0
uvicorn==0.48.0
wasabi==1.1.3
watchdog==5.0.3
weasel==0.4.3
websockets==16.0
wrapt==2.2.1
